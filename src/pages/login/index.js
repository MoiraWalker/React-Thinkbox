import './index.scss';
import { LoginForm } from "../../components/forms/organisms/loginForm"

export const Login = () => {
    return(
       <div className="page__container page__center">
            <LoginForm/>
       </div>
    );
};