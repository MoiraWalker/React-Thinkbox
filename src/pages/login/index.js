import './index.scss';
import { LoginForm } from "../../components/forms/organisms/loginForm"

export const Login = () => {
    return(
       <div className="page-container page-center">
            <LoginForm/>
       </div>
    );
};